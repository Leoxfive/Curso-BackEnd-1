<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
</head>
<body>
  <h1>{{title}}</h1>
  <h2>Lista de productos:</h2>
  {{#if products.length}}
    <ul>
      {{#each products}}
        <li>{{this.title}} - ${{this.price}}</li>
      {{/each}}
    </ul>
  {{else}}
    <p>No hay productos todavía.</p>
  {{/if}}
</body>
</html>